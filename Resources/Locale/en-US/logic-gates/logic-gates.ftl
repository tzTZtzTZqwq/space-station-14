logic-gate-examine = 它目前是一个{INDEFINITE($gate)} {$gate} 门。

logic-gate-cycle = 切换到 {INDEFINITE($gate)} {$gate} 门

power-sensor-examine = It is currently checking the network's {$output ->
    [true] 输出
    *[false] 输入
} battery.
power-sensor-voltage-examine = 它正在检查{$voltage}电力网络。

power-sensor-switch = Switched to checking the network's {$output ->
    [true] 输出
    *[false] 输入
} battery.
power-sensor-voltage-switch = 已将网络切换到{$voltage}！
